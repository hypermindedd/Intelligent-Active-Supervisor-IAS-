# IAS 2H_CHANGE — 2026-09-10 15:49 Tehran

## Delta since 14:15 Tehran

- `Main_protection` is now active on the default branch with required PRs, strict required status checks, zero required approvals, Code Owner review OFF, deletion/non-fast-forward protection, and no bypass actors.
- WS-01 PR #8 current head `17c5fac34b2189807a20368e544ae80b478983bc` passes Governance workflow run `34474811459`.
- PR #8 now contains all 25 required synthetic fixtures (`FX-001`..`FX-025`), `fixtures/index.json`, the Workflow & Event Contract, and `wdn-20260910-007-reconciliation.md`.
- The earlier HIGH Debugger finding for missing FX-021..FX-025 is resolved.
- WDN-20260910-007 corrections C1-C3 are materially applied on the PR branch, but are not authoritative until merge to `main`.
- WDN-20260910-001 remains ACTIVE. Gate Hardening v0.2 is not yet merged and the active lock still blocks release of WS-02..WS-11.

## New P0 CI enforcement defect

A positive control attempt on PR #10 exposed a required-status context mismatch:

- workflow run `34476286260` = SUCCESS;
- actual check run registered on the commit = `governance` (check run `102867727546`);
- active ruleset requires context = `IAS Governance Gate / governance`;
- GitHub merge API rejects the PR because that required context is still `expected`.

This is an enforcement availability defect: legitimate merges remain blocked even when Governance CI is green. WS-00 remains REVIEW until the ruleset/check context is reconciled and a positive control merge is proven.

## Authoritative workstream decisions

| Workstream | Gate | Progress | Decision |
|---|---|---:|---|
| WS-00 | REVIEW | 55% | Correct status-context mismatch, then complete Gate Hardening v0.2 and mandatory tests. No DONE claim. |
| WS-01 | GO | 65% | Contract work is authorized, but merge/freeze waits for the WS-00 status-context correction; not DONE until authoritative-branch verification. |
| WS-02..WS-11 | BLOCKED | 0% | No release. Dependency block plus active WARDEN_LOCK WDN-20260910-001. |

## Active locks

`WDN-20260910-001` — WARDEN_LOCK on downstream release (`WS-02`..`WS-11`). Release requires Gate Hardening v0.2, three required negative tests, and an explicit Warden UNLOCK referencing the directive.

## Priority

- P0: reconcile the ruleset required status context with the exact GitHub check-run context and prove a positive control merge.
- P0: Gate Hardening v0.2 + three negative tests + Warden UNLOCK.
- P1: finish WS-01 authoritative merge/freeze verification after the P0 CI context fix.
- P1: resolve outstanding human decisions such as repository visibility and missing future workstream ownership before those scopes start.

## Progress

Evidence-based overall progress: **10%** using current 12-workstream registry with WS-00=55%, WS-01=65%, and WS-02..WS-11=0%.

No downstream authorization is created by this report.
